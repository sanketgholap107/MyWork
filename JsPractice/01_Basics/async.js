/**
 * Default Javascript: Synchronus and single threaded  
 *  synchronus means it gets executed one by one.
 * 
 * Execution context: 
 *         - execute one line of code at a time
 *         - each operation waits for the last one to complete before executing
 *      
 */